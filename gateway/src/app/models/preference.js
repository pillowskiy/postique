/**
 * @typedef {Object} PreferencePosts
 * @property {import('#app/models').Post[]} items
 * @property {number} total
 * @property {number} page
 * @property {number} limit
 */

/**
 * @typedef {Object} PreferenceUsers
 * @property {import('#app/models').User[]} items
 * @property {number} total
 * @property {number} page
 * @property {number} limit
 */

/**
 * @typedef {Object} MuteResult
 * @property {boolean} muted
 */

export {};
